import { Container } from './Logout.styles';
import { ReloadIcon } from '@radix-ui/react-icons';

const Logout = () => {
  return (
    <Container>
      <ReloadIcon />
    </Container>
  );
};

export default Logout;
