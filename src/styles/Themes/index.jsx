import {
  violetDark,
  mauveDark,
  blueDark,
  redDark,
  greenDark,
} from '@radix-ui/colors';

const darkTheme = {
  colors: {
    ...violetDark,
    ...mauveDark,
    ...blueDark,
    ...redDark,
    ...greenDark,
  },
};

export { darkTheme };
